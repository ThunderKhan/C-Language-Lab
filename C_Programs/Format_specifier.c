#include <stdio.h>

int main() {
  printf("I am %d years old and He is %d years old\n", 20, 21);
  return 0;
}

/*
- The Matching of each number will happen automatically.
- For floating point values %f is used, for one values with just one decimal %.1f and for n decimals %.nf is used.
*/
